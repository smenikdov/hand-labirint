{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Docker Container",
            "type": "shell",
            "command": "docker run -it --rm -p 3000:3000 -e CHOKIDAR_USEPOLLING=true hand-labirint",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}